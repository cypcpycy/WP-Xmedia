=== 音乐资料库管理器 ===
Contributors: music-library-manager
Tags: music, audio, player, shortcode
Requires at least: 6.2
Requires PHP: 7.4
Stable tag: 0.14.35
License: GPLv2 or later

管理歌曲资料。WordPress 插件连接独立部署的远程音乐 API，完成 QQ 音乐搜索、授权、试听和资源解析；媒体文件仍由 WordPress 导入自己的媒体库。

歌曲编辑页右侧的“搜索歌曲资料”面板支持 QQ 扫码登录、四档音质、在线试听和一键添加到音乐库。导入时会自动保存音频、封面与 LRC 歌词附件，并填写歌曲名、作者、专辑、时长和来源地址。

远程封面、授权音频和歌词文件可在保存歌曲时通过 WordPress 官方媒体接口自动导入媒体库，歌曲记录保存附件 ID，并优先使用本地附件地址。

后台设置可控制默认自动导入与封面、音频、歌词文件容量上限；歌曲编辑页也可以直接从 WordPress 媒体库选择文件。

歌曲添加与编辑使用精简专用界面，不显示 Gutenberg 正文编辑器，只保留必要的歌曲资料、分类和保存操作。

在文章或页面的区块编辑器中添加“音乐播放器”区块，即可从下拉列表直接选择音乐库歌曲，无需查找 ID 或手写短代码。

音乐库单曲详情页会自动显示当前歌曲播放器，点击歌曲链接后可直接播放，无需在歌曲正文中额外插入短代码。

== 使用方法 ==
1. 部署随项目提供的音乐 API 服务；本地可使用 Docker，云端也可部署到独立服务器。
2. 上传并启用 WordPress 插件，在“音乐库 > 设置”导入 JSON 接口规则文件。
3. 进入“音乐库 > 添加歌曲”，首次使用时完成 QQ 扫码授权。QQ 登录状态保存在远程 API 服务器，与浏览器无关。
4. 选择音质并搜索；可试听、导入单曲，或点击专辑名称查看并批量导入整张专辑。
5. 整张专辑导入时会自动建立同名播放列表。也可在“音乐库 > 音乐播放列表”自行创建列表并给歌曲分组。
6. 使用 `[music id="歌曲ID" autoplay="no"]` 插入单曲、`[music_list limit="10" autoplay="no"]` 显示最新歌曲，或使用 `[music_playlist id="列表ID" autoplay="no"]` / `[music_playlist name="列表名称" autoplay="no"]` 插入整张播放列表。自动播放默认关闭；需要开启时将 `autoplay` 写为 `yes`（也接受 `true`、`1`、`on`）。浏览器仍可能按自身策略阻止未静音的自动播放。

播放器使用随插件分发的 APlayer 1.10.1（MIT License），不会在访客浏览页面时请求 CDN。歌曲标题、作者、封面、本地媒体附件与 LRC 歌词均会传入播放器；同一页面可放置多个 [music] 和 [music_list] 实例。[music] 可使用 lyrics="no" 关闭歌词。

== 更新日志 ==
= 0.14.35 =
* 前端播放器当前歌曲改为自动预载，AJAX返回后预连接媒体域名，并提前预载下一首歌曲。
* 切歌后继续预载后一首，降低CloudFront Range首字节延迟造成的等待感。
* 签名令牌对应的播放器数据缓存5分钟，减少重复访问时的WordPress查询耗时。

= 0.14.34 =
* WordPress更新页点击“再次检查”时主动清除GitHub Release与插件更新缓存，并立即执行插件更新检查。
* 修复WordPress 7.1仅强制检查核心、导致自定义GitHub插件不显示更新按钮的问题。

= 0.14.33 =
* 收录状态筛选改为完整音乐库口径，已收录数量包含当前歌单歌曲，两类数量之和等于全部有效歌曲。
* 当前歌单歌曲在已收录列表中标记并禁用重复添加；卡片补充音乐分类和所属歌单，搜索支持音乐分类。

= 0.14.32 =
* 歌单编辑页的音乐库候选区新增“未被歌单收录”和“已被歌单收录”两种筛选方式及数量。
* 已被其他歌单收录的歌曲显示所属歌单，并可继续加入当前歌单；搜索与分页保留收录状态。

= 0.14.31 =
* 歌单编辑页的已有歌曲新增音乐分类筛选和每页30首分页。
* 分类筛选或翻页后保存时，未显示歌曲通过隐藏字段保留，避免误从歌单移除并维持原顺序。

= 0.14.30 =
* 播放列表总览新增每页20个歌单的分页，并可按歌单名或歌单内歌曲名称、专辑、作者搜索。
* 歌单编辑页的候选歌曲新增名称、专辑、作者搜索，并保留每页30首分页和搜索条件。

= 0.14.29 =
* 永久删除歌曲及独占文件时，缺少附件ID的旧记录会通过媒体URL和文件名反查媒体库附件并清理。
* “只删除记录”模式仍保留音频、封面和歌词文件。

= 0.14.28 =
* 扫码轮询成功响应中的远程API提示会直接显示在后台，包括QQ音乐登录设备超限等账号错误。
* 远程API返回终止状态但未登录时停止轮询并恢复对应扫码按钮。

= 0.14.27 =
* 二维码启动和轮询失败时显示WordPress代理返回的具体错误，不再只显示“二维码获取失败”。
* 后台脚本使用全新文件名，避免错误提示修复被CDN旧缓存覆盖。

= 0.14.26 =
* 后台音乐搜索脚本改用全新文件名，绕过忽略查询参数的CDN缓存，确保双扫码登录逻辑生效。

= 0.14.25 =
* QQ音乐登录新增“网页扫码”和“QQ音乐客户端扫码”两种入口，并向远程API透传对应登录类型。
* 二维码失效或请求失败后正确恢复对应登录按钮状态与文字。

= 0.14.24 =
* 点击WordPress“再次检查”时同步清除GitHub Release缓存，立即检测新版本，不再等待6小时。

= 0.14.23 =
* API规则导入允许使用 `localhost` 和 `127.0.0.1` 本机接口地址，方便本地开发与Docker外部端口连接。

= 0.14.22 =
* 前端播放器HTML不再包含音频、封面和歌词地址，改用签名令牌通过AJAX按需加载歌曲数据。
* 播放器令牌使用站点密钥签名，防止访客自行篡改歌曲集合。

= 0.14.21 =
* 音乐库列表新增文章引用统计列，并显示已引用、未引用歌曲总数。
* 新增文章引用状态筛选，支持单曲、歌单、音乐列表短代码及音乐区块引用关系。

= 0.14.20 =
* 歌单编辑页的可加入歌曲仅显示尚未加入任何歌单的音乐，隐藏已属于当前或其他歌单的歌曲。

= 0.14.19 =
* Hermit X 导入优先按来源 ID 精确去重，允许同名同歌手但来源不同的版本独立导入。
* 没有来源 ID 和旧记录 ID 时，回退规则增加专辑字段，避免同名不同专辑被合并。

= 0.14.18 =
* 播放器样式改用全新文件名，绕过服务器/CDN对旧 CSS 路径的强缓存，确保第一首残留焦点修复生效。
* 歌词仅按 LRC 的标题、歌手、专辑和声明时长生成严格指纹，与音乐库四项完全一致且结果唯一时才关联，不使用误差范围。

= 0.14.17 =
* 同名歌曲的歌词映射新增专辑名称与音频时长判定，只在结果唯一时自动关联。
* 优先读取 LRC 专辑与长度标签，缺少长度标签时使用最后歌词时间戳，并允许 5 秒时长误差。
* 同时包含 0.14.16 的播放列表旧焦点高亮修复。

= 0.14.16 =
* 修复播放列表切换歌曲后第一首仍然保持灰色焦点高亮的问题。

= 0.14.15 =
* 批量媒体上传改为浏览器自动分批处理，绕过服务器默认每次最多 20 个文件的限制。
* 新增明细进度条，显示总数、已处理、上传、歌词匹配、跳过及失败数量。
* 支持中断后重新选择同批文件继续；已有同名歌词不重复上传，并重新按音乐库名称尝试匹配。

= 0.14.14 =
* 歌词匹配仅依据 LRC 文件名主体与音乐库歌曲名称，不依赖附件 ID 或其他媒体字段。
* 名称比较统一全角半角与大小写，并忽略空格、标点及符号差异，提高中文歌词匹配率。

= 0.14.13 =
* 修复 Hermit 逗号分隔的多音乐集合只识别首个旧歌曲 ID、未创建歌单的问题。
* 一篇文章中的多个音乐集合分别建立歌单，并保持原位置和原有顺序写回文章。

= 0.14.12 =
* 歌词整理改为从歌曲记录的歌词附件或歌词 URL 定位 uploads 任意年月子目录中的 LRC，再移动到专用歌词目录。
* 未被歌曲记录关联的文件不扫描不移动；多个歌曲共享同一源文件时标记为关联不唯一并保持原位。
* 核心运行类换代为 V9，避免服务器 OPcache 继续执行旧版核心文件。

= 0.14.11 =
* 设置页新增歌词文件整理工具，支持预览与确认执行，将插件歌词目录内的文件安全重命名为对应歌曲标题。
* 重命名同步更新歌曲歌词 URL、附件路径、附件元数据和当前站点域名 GUID；同名文件稳定避让，孤立或关联不唯一的文件保持不动。
* 核心运行类换代为 V8，避免服务器 OPcache 继续执行旧版核心文件。

= 0.14.10 =
* 完善 Hermit 多曲短代码迁移回归：自动创建有序歌单、继承自动播放并幂等复用同源歌单。
* 映射不完整时预览明确显示无映射或多重映射的旧 ID，迁移保留原文且不创建残缺歌单。
* 核心运行类换代为 V7，避免服务器 OPcache 继续执行旧版核心文件。

= 0.14.9 =
* Hermit 数据导入预览自动过滤音乐库已有歌曲，并显示文件总数、过滤数量和待导入数量。
* 去重依次使用 Hermit 来源 ID、旧记录 ID、规范化歌曲名与歌手，不依赖媒体附件 ID。
* 后台导入脚本和核心运行类使用全新文件名，绕过 CDN 与 OPcache 旧缓存。

= 0.14.8 =
* Hermit 文章批量迁移预览逐条显示继承后的自动播放状态；单曲和自动歌单均保留原短代码设置。
* 核心运行类换代为 V5，避免服务器 OPcache 继续执行旧版核心文件。

= 0.14.7 =
* Hermit 多曲短代码迁移时自动创建有序播放列表，完整保留旧歌曲顺序与自动播放设置。
* 自动歌单支持幂等复用、来源审计、失败回滚；前端和后台播放器统一遵循歌单曲目顺序。
* 核心运行类换代为 V4，避免服务器 OPcache 继续执行旧版核心文件。

= 0.14.6 =
* 精确恢复 Hermit/APlayer 控制栏布局，使进度条、时间和右侧按钮在不同宽度下保持对齐且不越界。
* 前端播放器 CSS 使用全新静态文件名，绕过忽略查询参数的 CDN 缓存。

= 0.14.5 =
* 播放器短代码在文章内容过滤链结束时才渲染，隔离旧播放器插件对音频与封面数据的篡改。
* 核心运行类换代为 V3，避免服务器 OPcache 继续执行旧版核心文件。

= 0.14.4 =
* 修复经典编辑器预览卡片被保存进文章正文的问题；保存与前端显示会安全恢复为插件短代码。
* 经典编辑器静态脚本改用全新文件名，避免 CDN 忽略查询参数时继续返回旧脚本。

= 0.14.3 =
* 新增短代码独立自动播放控制、Hermit 文章短代码安全批量迁移和 90px Hermit/APlayer 前端样式。
* 提升静态资源版本以刷新前端播放器 CSS 和 JavaScript 缓存。

= 0.13.1 =
* 设置页移除手工 API 地址和 GitHub Token 输入框，只通过 JSON 规则文件配置远程接口。
* 保存媒体设置时保留已导入规则中的 API 地址。

= 0.13.0 =
* 接入 GitHub Release 在线更新，在 WordPress 插件列表显示原生版本提示、详情与立即更新操作。
* 通过公开 GitHub 仓库免配置检查和下载新版本。

= 0.12.0 =
* 新增 JSON 接口规则导入，自动配置 API 地址、搜索/资源接口和完整 QQ 扫码登录链路。
* 搜索接口透传后端分页信息，便于后台和其他调用方统一渲染页码。

= 0.11.0 =
* 文章编辑器新增可视化音乐库：封面、作者、专辑、试听及“加入文章”操作。
* 新增可视化播放列表区块，可预览列表封面、包含曲目和试听后插入文章。

= 0.10.6 =
* 关闭音乐搜索弹窗时立即暂停全部单曲和专辑播放器，并将播放进度归零。

= 0.10.5 =
* 专辑 APlayer 为全部曲目加载同步歌词，切换歌曲时自动切换对应歌词。

= 0.10.4 =
* 每条搜索结果改为统一的歌曲外层卡片，播放器和操作区作为卡片内部区域；同步优化手机端上下布局。

= 0.10.3 =
* 搜索结果改为播放器与操作栏两块独立圆角卡片，统一轻阴影并移除突兀的竖向分割线。

= 0.10.2 =
* 修复歌词区域高度覆盖原生控制栏的问题，确保进度条始终位于播放器卡片内部并可见。

= 0.10.1 =
* 右侧独立操作栏改为与播放器一致的白色背景，并弱化分隔线和无专辑状态。

= 0.10.0 =
* 将专辑和添加按钮彻底移出 APlayer，播放器恢复独立完整布局和原生控制区域。

= 0.9.9 =
* 恢复并强化搜索播放器进度条、时间、循环和音量等基础控制，避免被右侧操作区遮挡。

= 0.9.8 =
* 增加桌面、平板和手机三段响应式布局；手机端采用全屏搜索、完整播放器和并排触控按钮。

= 0.9.7 =
* 右侧操作栏、播放器和歌词预留区域改为按容器宽度比例响应式缩放。
* 专辑按钮悬停提示直接显示完整专辑名称。

= 0.9.6 =
* 进一步收窄右侧操作栏；专辑名称使用小号字体和省略显示，悬停可查看完整名称。

= 0.9.5 =
* 移除搜索结果中重复作者信息，右侧改为专辑链接与添加按钮上下排列的紧凑操作区。

= 0.9.4 =
* 压缩单曲播放器高度，将专辑链接和小号添加按钮并入播放器右侧。

= 0.9.3 =
* 单曲搜索结果升级为带进度、音量、循环控制和同步歌词的完整 APlayer。

= 0.9.2 =
* 修复单曲播放器信息区高度不足，确保专辑链接和“添加到音乐库”按钮完整显示。

= 0.9.1 =
* 搜索结果改为紧凑的单列纵向 APlayer，专辑与添加按钮嵌入播放器信息区。
* 专辑曲目支持逐首勾选、全选和取消全选，批量操作只导入已选择的歌曲。

= 0.9.0 =
* 后台搜索结果统一改用插件内置 APlayer，操作按钮融入播放器卡片。
* 专辑详情改用 APlayer 专辑列表模式，可切歌、连续试听并在确认后批量导入。

= 0.8.1 =
* 点击专辑名称后先展示全部曲目和逐首播放器，试听确认后再批量导入。
* 整张专辑导入完成后自动跳转到对应的 WordPress 音乐播放列表页面。

= 0.8.0 =
* 回退到稳定的独立音乐 API 登录与资源解析架构，WordPress 通过可配置的远程 API 地址连接。
* 保留专辑展开、整张专辑批量导入、自动播放列表和自定义播放列表功能。
* 远程音乐 API 新增专辑全部曲目接口。

= 0.7.3 =
* 在整个二维码轮询过程中累计并持久化 QQ 会话 Cookie，修复手机确认后无法取得 p_skey 的问题。

= 0.7.2 =
* 兼容 QQ 二维码轮询接口新的回调参数格式、单双引号和 HTML 实体编码。

= 0.7.1 =
* 修复 QQ 扫码确认后 OAuth 阶段缺少二维码会话 Cookie 导致页面持续等待的问题。
* 登录轮询失败时立即停止并显示具体错误，不再无提示卡住。

= 0.7.0 =
* 将搜索、QQ 扫码授权、音频地址、歌词与媒体导入全部改写为 PHP，不再依赖 Docker 或外部本地服务。
* 搜索结果可点击专辑名称查看全部曲目，并可批量导入整张专辑。
* 批量导入自动建立专辑播放列表；支持自定义播放列表与整表短代码。

= 0.6.10 =
* 修复更新歌曲时误把 WordPress 媒体库本地 URL 当作远程文件重复下载的问题。

= 0.6.9 =
* 后台隐藏歌词文本、歌词文件地址和歌词附件状态；歌词仍自动保存并用于前台播放。

= 0.6.8 =
* 修正歌曲资料顶部布局，媒体库状态固定显示在封面右侧。

= 0.6.7 =
* 搜索结果直接显示懒加载播放器，移除额外的“试听”按钮。

= 0.6.6 =
* 重排歌曲资料界面：封面和媒体库状态置顶，隐藏来源 URL 字段并优化响应式排版。

= 0.6.5 =
* 音乐搜索弹窗增加真实接口分页，可浏览上一页和下一页结果。

= 0.6.4 =
* 优先导入 QQ 专辑无尺寸原图，原图不存在时再按 1500、800、500、300 像素回退。

= 0.6.3 =
* QQ 专辑封面按 1500、800、500、300 像素依次回退，自动导入可用的最大清晰版本。

= 0.6.2 =
* 将后台歌曲搜索改为大尺寸弹窗，避免编辑页面过长和结果区域拥挤。

= 0.6.1 =
* 规范新导入媒体的文件名：音频和歌词使用歌曲名称，封面使用专辑名称。

= 0.6.0 =
* 在歌曲编辑页集成 QQ 音乐授权搜索、音质选择和在线试听。
* 搜索结果仅保留“试听”和“添加到音乐库”。
* 一键导入歌曲、封面、LRC 歌词以及完整元数据到 WordPress 媒体库。
* 初版曾使用独立音乐服务；0.7.0 起已由纯 PHP 实现取代。

= 0.4.0 =
* 使用本地托管的官方 APlayer 1.10.1 发行资源替换 WordPress 原生音频控件。
* 支持封面、作者、内嵌 LRC 歌词、同页多实例与移动端响应式布局。
* 保持 [music] 与 [music_list] 原有参数兼容，并加强播放器数据的安全转义。

== 版权说明 ==
搜索结果、试听地址、封面和歌词仍受其提供方条款约束。插件只使用当前授权账号正常获得的资源，不规避 DRM、会员鉴权或其他访问控制；站点管理员应确保所发布内容拥有合法授权。
